# Prints a string literal
puts 'I will now count my chickens:'

# Calculates the number of hens and prints it
puts "Hens #{25 + 30 / 6.0}"
# Calculates the number of roosters and prints it
puts "Roosters #{100.0 - 25 * 3 % 4}"

# Prints a string literal
puts 'Now I will count the eggs:'

# Prints the result of the expression
puts 3 + 2 + 1 - 5 + 4 % 2 - 1.0 / 4 + 6

# Prints a string literal
puts 'Is it true that 3 + 2 < 5 - 7?'

# Prints the result of comparing two calculations
puts 3.0 + 2.0 < 5.0 - 7.0

# Prints a string followed by the result of a calculation
puts "What is 3 + 2? #{3.0 + 2.0}"
# Prints a string followed by the result of a calculation
puts "What is 5 - 7? #{5.0 - 7.0}"

# Prints a string literal (uses double quotes because of the apostrophes)
puts "Oh, that's why it's false."

# Prints a string literal
puts 'How about some more.'

# Prints a string followed by the result of a comparison
puts "Is it greater? #{5.0 > -2}"
# Prints a string followed by the result of a comparison
puts "Is it greater or equal? #{5 >= -2.0}"
# Prints a string followed by the result of a comparison
puts "Is it less or equal? #{5.0 <= -2.0}"
